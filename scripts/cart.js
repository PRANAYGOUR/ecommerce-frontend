const cartItems=document.getElementById("cart-items"),cartTotal=document.getElementById("cart-total");function renderCart(){const a=getCart();cartItems.innerHTML="";let b=0;if(!a.length)return cartItems.innerHTML="<p>Your cart is empty</p>",void(cartTotal.textContent="0");a.forEach((a,c)=>{b+=a.price*a.qty,cartItems.innerHTML+=`<div class="cart-item-card"><img src="${a.image}" loading="lazy"><div class="cart-item-details"><h4>${a.title}</h4><p>₹ ${a.price}</p></div><div class="cart-item-quantity-controls"><button onclick="change(${c},-1)">−</button><span>${a.qty}</span><button onclick="change(${c},1)">+</button></div><button class="remove-item-btn" onclick="remove(${c})">Remove</button></div>`}),cartTotal.textContent=b.toFixed(0)}function change(a,b){const c=getCart();c[a].qty+=b,c[a].qty<=0?remove(a):(saveCart(c),renderCart())}function remove(a){const b=getCart();b.splice(a,1),saveCart(b),renderCart()}renderCart();
