const emailInput=document.getElementById("email"),passwordInput=document.getElementById("password"),confirmPassword=document.getElementById("confirm-password"),submitBtn=document.getElementById("submit-btn"),toggleForm=document.getElementById("toggle-form"),formTitle=document.getElementById("form-title"),errorMsg=document.getElementById("error-msg");let isLogin=!0;toggleForm.onclick=a=>{a.preventDefault(),isLogin=!isLogin,formTitle.textContent=isLogin?"Login":"Sign Up",submitBtn.textContent=isLogin?"Login":"Sign Up",confirmPassword.style.display=isLogin?"none":"block",errorMsg.textContent=""};document.getElementById("auth-form").addEventListener("submit",a=>{a.preventDefault();const b=emailInput.value.trim(),c=passwordInput.value.trim(),d=confirmPassword.value.trim();return b&&c?(isLogin||c===d?(isLogin?auth.signInWithEmailAndPassword(b,c):auth.createUserWithEmailAndPassword(b,c)).then(()=>window.location.href="index.html").catch(a=>errorMsg.textContent=a.message):void(errorMsg.textContent="Passwords do not match")):void(errorMsg.textContent="All fields required")});
